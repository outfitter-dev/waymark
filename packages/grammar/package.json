{
  "name": "@waymarks/grammar",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "dist/index.d.ts",
  "description": "Minimal, stable grammar parser for waymark syntax",
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "bun run build:types && bun build src/index.ts --outdir dist --target bun",
    "build:types": "tsc -p tsconfig.build.json --emitDeclarationOnly",
    "dev": "bun build src/index.ts --outdir dist --target bun --watch",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "bunx ultracite check",
    "check:waymarks": "echo 'Waymarks check will be implemented in v1.0'"
  },
  "devDependencies": {
    "@types/bun": "1.2.22"
  }
}
