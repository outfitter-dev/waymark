// tldr ::: global test setup for Bun test runner

import { afterAll, beforeAll } from "bun:test";

beforeAll(() => {
  // todo ::: add global test setup when needed
});

afterAll(() => {
  // todo ::: add global test cleanup when needed
});
